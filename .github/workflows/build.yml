name: Build and Attest

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  id-token: write
  contents: read
  attestations: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos: [linux, windows]
        goarch: [amd64, arm64]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.21'

    - name: Build
      env:
        GOOS: ${{ matrix.goos }}
        GOARCH: ${{ matrix.goarch }}
      run: |
        OUTPUT_NAME=jenkins-monitor-${{ matrix.goos }}-${{ matrix.goarch }}
        if [ "${{ matrix.goos }}" = "windows" ]; then
          OUTPUT_NAME="${OUTPUT_NAME}.exe"
        fi
        go build -v -o $OUTPUT_NAME cmd/jenkins-monitor/main.go

    - name: Generate SBOM
      uses: anchore/sbom-action@v0
      with:
        path: .
        format: spdx-json
        output-file: jenkins-monitor-${{ matrix.goos }}-${{ matrix.goarch }}.spdx.json
        upload-artifact: false

    - name: Attest Build Provenance
      uses: actions/attest-build-provenance@v1
      with:
        subject-path: |
          jenkins-monitor-${{ matrix.goos }}-${{ matrix.goarch }}*
        sbom-path: jenkins-monitor-${{ matrix.goos }}-${{ matrix.goarch }}.spdx.json

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: jenkins-monitor-${{ matrix.goos }}-${{ matrix.goarch }}
        path: |
          jenkins-monitor-${{ matrix.goos }}-${{ matrix.goarch }}*
          jenkins-monitor-${{ matrix.goos }}-${{ matrix.goarch }}.spdx.json
        if-no-files-found: error
        retention-days: 1
